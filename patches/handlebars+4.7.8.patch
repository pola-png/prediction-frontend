diff --git a/node_modules/handlebars/lib/index.js b/node_modules/handlebars/lib/index.js
index 095f95f..a5493a9 100644
--- a/node_modules/handlebars/lib/index.js
+++ b/node_modules/handlebars/lib/index.js
@@ -1,5 +1,3 @@
-import * as fs from 'fs';
-
 import Handlebars from './handlebars.js';
 
 // Environment detection.
@@ -14,14 +12,4 @@ const deletes = [];
 for (const key in Handlebars) {
   deletes.push(key);
 }
-
-if (Handlebars.bestContext && !Handlebars.compile) {
-  // We're being loaded via require('./handlebars')
-  Handlebars.JavaScriptCompiler = Object; // eslint-disable-line no-undef
-  Handlebars.precompile = function () {};
-  Handlebars.template = function () {};
-  require.extensions['.handlebars'] = function (module, filename) {
-    const template = fs.readFileSync(filename, 'utf8');
-    module.exports = Handlebars.compile(template);
-  };
-}
 export default Handlebars;
