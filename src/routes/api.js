const express = require('express');
const router = express.Router();

// Placeholder for future routes
router.get('/', (req, res) => {
    res.json({ message: 'Welcome to the GoalGazer API' });
});

module.exports = router;
