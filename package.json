
{
  "name": "goalgazer-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:*",
    "dev:backend": "npm run dev --workspace=backend"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
